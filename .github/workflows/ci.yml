name: CI

permissions:
  contents: read

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  # Opt-in to newer GitHub-hosted runner JavaScript runtime
  # to avoid deprecation warnings/errors for actions still running on Node.js 20.
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v7
        with:
          version: "latest"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14.2"
      - name: Set up Python venv for uv
        run: uv venv
      - name: Install dependencies
        run: |
          source .venv/bin/activate
          uv pip install ruff mypy
      - name: Run ruff
        run: |
          source .venv/bin/activate
          ruff check .
      - name: Run ruff format check
        run: |
          source .venv/bin/activate
          ruff format --check .

  type-check:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v7
        with:
          version: "latest"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14.2"
      - name: Set up Python venv for uv
        run: uv venv
      - name: Install dependencies
        run: |
          source .venv/bin/activate
          uv pip install -e ".[dev]"
      - name: Run mypy
        run: |
          source .venv/bin/activate
          mypy src/

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v7
        with:
          version: "latest"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14.2"
      - name: Set up Python venv for uv
        run: uv venv
      - name: Install dependencies
        run: |
          source .venv/bin/activate
          uv pip install -e ".[dev]"
      - name: Run tests
        run: |
          source .venv/bin/activate
          pytest --cov=storzandbickel_ble --cov-report=xml --cov-report=term
      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-umbrella

